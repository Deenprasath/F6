#include <stdio.h>

int prime (int n)
{
    for (int i=2;i<=sqrt(n);i++)
    {
        if(n%i==0)
        {
            return 0;
        }
    }
    return 1;
    
}
int main()
{
    int n;
    scanf("%d",&n);
    if (prime(n))
    {
        printf("\n prime");
    }
    else
    {
        printf("\n not");
        
    }
    return 0;
}
